openapi: 3.0.0
info:
  title: Frozen Vault AppRole secret-id migration slice
  version: old
paths:
  /v1/auth/approle/role/{role_name}/secret-id/{secret_id}:
    get:
      parameters:
        - in: path
          name: role_name
          required: true
          schema: {type: string}
        - in: path
          name: secret_id
          required: true
          schema: {type: string}
      responses:
        '200': {description: Secret ID metadata}
    delete:
      parameters:
        - in: path
          name: role_name
          required: true
          schema: {type: string}
        - in: path
          name: secret_id
          required: true
          schema: {type: string}
      responses:
        '204': {description: Secret ID destroyed}
