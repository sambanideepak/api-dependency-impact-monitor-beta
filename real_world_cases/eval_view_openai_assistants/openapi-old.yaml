openapi: 3.0.3
info:
  title: Reduced Assistants API migration fixture
  version: old
paths:
  /v1/threads:
    post:
      operationId: createThread
      responses:
        '200':
          description: Thread created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
